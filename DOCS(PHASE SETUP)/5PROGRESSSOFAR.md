ACHIEVEMENTS (The Foundation)Project Infrastructure: You have initialized a Turbo Monorepo with a functional workspace layout: apps/web, apps/functions, and packages/shared.API Readiness: You have enabled the core "heavy lifter" APIs in Google Cloud, including Firestore, Vertex AI, and Cloud Run Admin.Security Architecture:Workload Identity Federation (WIF): You've moved away from risky JSON keys to a modern, keyless GitHub-to-GCP trust relationship.Lean Security: You've pivoted to a standard Firebase Auth model to reduce complexity while maintaining user-scoped data protection.Data Contracts: You have started the packages/shared folder with Zod schemas for Users, ensuring both frontend and backend stay in sync.Backend Hardening: You've resolved the ES Module/CommonJS conflict by correctly configuring .eslintrc.cjs, allowing ESLint to run in a "type": "module" environment.Firestore Configuration: You have the firestore.rules drafted to enforce user ownership and protect the medical corpus.🚀 NEXT STEPS (The Implementation)Since your partner is focusing on the PDF corpus and ingestion, your path forward is divided into building the "UI shell" and the "API pipes."1. Finalize the Shared Data ModelsExpand packages/shared to include the Message and Chat schemas.Goal: Define the parentMessageId field to support your "Branching" requirement and the sources array for RAG citations.2. Implement the "Agent" (Backend)Inside apps/functions, create the RAG orchestrator.Task: Write the findNearest() logic using the Firestore Admin SDK.Task: Set up the Abstention Gate ($threshold < 0.75$) to prevent the assistant from guessing on non-Zambian medical topics.3. Build the "Shell" (Frontend)Inside apps/web, build the interface for the Diabetes Assistant.Task: Create the Firebase Auth context to manage login states.Task: Build the Message Branching UI. This allows a user to toggle between v1 and v2 of a response by looking up siblings in the message tree.Task: Connect the tRPC Client to the local Firebase Emulator so you can start testing your partner's backend logic as soon as it's ready.4. Seeding & Local TestingTask: Once the corpus script is ready, run a "Seed" command to populate your local Firestore Emulator with a few sample diabetes chunks.Task: Verify the Soft Delete logic by ensuring the UI hides messages where deletedAt is not null.
